


document.addEventListener('DOMContentLoaded', () => {
   let inputs = document.querySelectorAll('.js-plate-for-sale-input');

   if (inputs.length) {
       inputs.forEach(input => {
          input.addEventListener('change', () => {
             let value = input.value;
             

          });
       });
   }
});